[tool.poetry]
name = "gui-condetrol"
version = "1.0.3"
description = ""
authors = ["damien <damien.bloch@institutoptique.fr>"]
readme = "README.md"
packages = [{include = "caqtus"}]

[tool.poetry.dependencies]
python = "^3.11, <3.13"

pyside6 = "^6.6.2"
qtawesome = "^1.3.0"

session = {path = "../../session"}
device = {path = "../../device"}
types = {path = "../../types"}
experiment-control = {path = "../../experiment_control"}
gui-common = {path = "../gui.common"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
