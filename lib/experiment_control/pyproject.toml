[tool.poetry]
name = "experiment-control"
version = "1.0.1"
description = ""
authors = ["damien <damien.bloch@institutoptique.fr>"]
readme = "README.md"
packages = [{include = "caqtus"}]

[tool.poetry.dependencies]
python = "^3.11"

numpy = "^1.26.4"
tblib = "^3.0.0"
anyio = {extras = ["trio"], version = "^4.3.0"}

device = {path = "../device"}
session = {path = "../session"}
shot-compilation = {path = "../shot_compilation"}
types = {path = "../types"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
