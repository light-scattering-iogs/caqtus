[tool.poetry]
name = "experiment-session"
version = "0.1.0"
description = ""
authors = ["Damien Bloch <bloc.dam@gmail.com>"]
readme = "README.md"
packages = [{include = "experiment"}]

[tool.poetry.dependencies]
python = "^3.8"
asyncpg = "^0.27.0"
platformdirs = "^3.2.0"

sqlalchemy = {extras = ["asyncio"], version = "^2.0.4"}

sql_model = {path = "../../sql_model", develop = true}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
