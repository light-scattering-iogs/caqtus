[tool.poetry]
name = "experiment-control-manager"
version = "0.1.0"
description = ""
authors = ["Damien Bloch <damien.bloch@institutoptique>"]
readme = "README.md"
packages = [{include = "experiment_control"}]

[tool.poetry.dependencies]
python = "^3.11, <3.13"

experiment-session = {path = "../../common/experiment/experiment.session", develop = true}
experiment-control-sequence-runner = {path = "../experiment_control.sequence_runner", develop = true}


[tool.poetry.group.dev.dependencies]
black = "^23.7.0"
mypy = "^1.4.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
