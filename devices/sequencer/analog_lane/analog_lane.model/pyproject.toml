[tool.poetry]
name = "analog-lane-model"
version = "0.1.0"
description = ""
authors = ["Damien Bloch <damien.bloch@institutoptique>"]
readme = "README.md"
packages = [{include = "analog_lane"}]

[tool.poetry.dependencies]
python = "^3.11, <3.13"

analog-lane-configuration = {path = "../analog_lane.configuration", develop = true}
lane-model = {path = "../../../../common/lane/lane.model", develop = true}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
