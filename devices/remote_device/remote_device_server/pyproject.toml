[tool.poetry]
name = "remote-device-server"
version = "0.1.0"
description = ""
authors = ["Damien Bloch <damien.bloch@institutoptique>"]
readme = "README.md"
packages = [{include = "remote_device_server"}]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^1.10.2"
device = { path = "../../device/", develop = true }


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
