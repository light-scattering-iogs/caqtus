[tool.poetry]
name = "camera-runtime"
version = "0.1.0"
description = ""
authors = ["Damien Bloch <bloc.dam@gmail.com>"]
readme = "README.md"
packages = [{include = "camera"}]

[tool.poetry.dependencies]
python = "^3.11"

attrs = "^23.1.0"
numpy = "^1.23.4"

camera-configuration = {path = "../camera.configuration", develop = true}
device-runtime = {path = "../../../device/device.runtime", develop = true}
image-types = {path = "../../../../common/types/image_types", develop = true}
log-exception = {path = "../../../../common/log_exception", develop = true}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
